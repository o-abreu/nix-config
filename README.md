# NixOS Configuration

My personal NixOS configuration, so far poorly documented. It implements TMPFS
impermanence combined with LUKS encryption, swap-based hibernation and SOPS
secret management. Surprisingly, this is the only setup of the kind I've seen so
far, so it might be of use to you too.

